interface:
  display_name: "NerdOut Journal"
  short_description: "Journal decisions in NerdOut and recall them in later tasks"
  default_prompt: "Use $nerd-out-notes:nerd-out-journal to configure the global workspace, recall prior journal notes, or journal this task."
policy:
  allow_implicit_invocation: true
