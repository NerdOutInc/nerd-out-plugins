interface:
  display_name: "Recall Journal"
  short_description: "Configure and use a Project-aware Recall journal"
  default_prompt: "Use $recall:recall-journal to configure this filesystem project or a global journal destination, optionally choose a Recall Project, and journal this task."
policy:
  allow_implicit_invocation: true
