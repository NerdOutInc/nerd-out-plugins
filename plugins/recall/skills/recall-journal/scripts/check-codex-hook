#!/bin/sh
set -eu

script_directory=$(CDPATH= cd "$(dirname "$0")" && pwd)
plugin_root=$(CDPATH= cd "$script_directory/../../.." && pwd)

exec "$plugin_root/bridge/recall-node" \
  "$script_directory/codex-hook-status.mjs" "$@"
