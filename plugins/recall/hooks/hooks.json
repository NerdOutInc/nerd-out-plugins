{
  "description": "Activate opted-in Recall journaling — recall plus live progress entries — for meaningful tasks.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/bin/sh \"${CLAUDE_PLUGIN_ROOT}/bridge/recall-node\" \"${CLAUDE_PLUGIN_ROOT}/hooks/journal-context.mjs\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
