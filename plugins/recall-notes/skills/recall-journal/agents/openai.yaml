interface:
  display_name: "Recall Journal"
  short_description: "Journal decisions in Recall and retrieve them in later tasks"
  default_prompt: "Use $recall-notes:recall-journal to configure the global workspace, bind this project to its own workspace, recall prior journal notes, or journal this task with live progress."
policy:
  allow_implicit_invocation: true
